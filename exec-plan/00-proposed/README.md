# 00-proposed — Candidate proposals

The `surveyor` agent (or a promoted proposal) writes task proposals here. Tasks in this folder are candidates only — nothing runs automatically from here. A human reviews and promotes a proposal to the Up-Next queue (via the `arbiter promote` CLI or dashboard drag) to make it eligible for execution.
