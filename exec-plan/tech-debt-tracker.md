# Tech-debt tracker

Auto-appended by `reviewer` (when it spots a shortcut it allows through) and read by
`surveyor` (to propose paydown when the queue is idle). One row per item.

| Date | Task | Item | Severity | Suggested fix |
|------|------|------|----------|---------------|
| _(none yet)_ | | | | |
