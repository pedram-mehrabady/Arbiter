# 02-incubating — Planning in progress

Planning runs here. The pipeline agents (reframe → question → research → design → integrator → plan) accumulate artifacts inside the task folder: `spec.md`, `0-reframe.md`, `1-questions.md`, `2-research.md`, `3-design.md`, `integration.md`, `5-plan.md`, `5-plan.json`. Once the plan agent writes `5-plan.json`, the conductor moves the task to `03-building/`.
