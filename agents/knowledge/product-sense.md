# Product sense (engineering KB)

Injected into: `reframe`, `question`, `design`, `surveyor`.

- **Who uses it:** {{TARGET_USERS}}
- **Find the 10x-simpler version.** Challenge the premise before designing. The best
  feature is often a smaller one that an existing module already half-covers.
- **Reuse over invent.** Check the component/service registry and sibling apps first
  (port existing UI rather than design fresh).
- **{{COMPLIANCE_CONTEXT}} shapes everything.** A "nice" feature that can't pass the
  {{COMPLIANCE_CONTEXT}} security evaluation is not shippable.

Canonical sources: `{{DOMAIN_SPEC_DOC}}`,
`{{DOMAIN_SPEC_DOC}}`.
